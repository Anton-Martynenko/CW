function pointsNumber(radius){
  let points = 0;
  for (let i = -radius; i <= radius; i++){
    for (let j = -radius; j <= radius; j++){
      if (i * i + j * j <= radius * radius) points++
    }
  }
  return points;
}
