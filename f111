function dataReverse(data) {
  if (data.length === 0) return []
  let arrayBytes = [];
  let tam = data.length / 8;
  for (let i = 0; i < tam; i++) {
    arrayBytes.push(data.splice(0,8));
  }
  return arrayBytes.reverse().join().split(',').map(v => v * 1);
}
