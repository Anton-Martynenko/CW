function triangleType(a, b, c){
  let result = Math.max(a,b,c);  
  if (a + b + c <= 2 * result) return 0;
  if (2 * result * result == a * a + b * b + c * c) return 2;
  if (2 * result * result > a * a + b * b + c * c) return 3;
  return 1;
}
