function shortcut (string) {
  let vowels = 'aeiou';
  let result = '';
  for (let i = 0; i < string.length; i++) {
    if (vowels.indexOf(string[i]) === -1) {
      result += string[i];
    }
  }
  return result;
}
